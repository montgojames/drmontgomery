The label/headline ratio from the last fix is correct — keep that
relationship exactly as-is. Two things now:

## 1. Scale label + headline up together, ~50%
Both are too small now in absolute terms, even though the ratio between them
is right. Increase both the small-caps label font-size AND the headline
font-size by roughly the same multiplier (~50% larger than current) so the
proportion between them stays fixed while the whole pairing gets bigger.
Apply to all 3 section header pairs (During Your Stay, Decisions & Next
Steps, Medical Conditions).

## 2. Increase card text, ~50%
Card title and description text (inside all 9 homepage cards) also needs to
increase by roughly 50% — same reasoning as before, too small for this
audience. Keep the current proportion between card title size and
description size, just scale both up together.

After both changes, sanity check that cards haven't become cramped or
mis-aligned now that the text inside them is larger — card padding/height
may need to adjust to accommodate the bigger text cleanly, not just have
text overflow tighter into the same box size.

Leave spacing-between-sections and nav bar font for the next round — still
doing these one at a time.
